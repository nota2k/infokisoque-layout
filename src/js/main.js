/**
 * Fichier JavaScript principal du thème
 */

// Import jQuery depuis WordPress
import $ from 'jquery';
import 'magic-grid';
// Import des modules JavaScript si nécessaire
import './layout.js';

// Initialisation du thème
$(document).ready(function() {
    console.log('Thème InfoKiosque chargé');
    
    // Initialisation des composants ici
    // initNavigation();
    // initModals();
    // initSliders();
});

// Export pour utilisation dans d'autres modules si nécessaire
// export { initNavigation, initModals };
